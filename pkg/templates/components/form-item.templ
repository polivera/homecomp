package components

templ FormInput(data InputData, inputFunc func(InputData) templ.Component) {
	<div class="flex flex-col mb-4">
		<label
			for={ data.ID }
			class="text-sm mb-2"
		>{ data.Label }</label>
		@inputFunc(data)
	</div>
}
